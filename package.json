{
  "name": "luffytv-miruro-api",
  "version": "7.2.0",
  "description": "LuffyTV API — Anidap + KAA + MKissa providers, AniList ID routing (no slug needed), HLS proxy, m3u8/mp4 sources",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "express": "^4.21.0"
  },
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  }
}
